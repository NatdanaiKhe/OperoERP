info:
  name: Delete Department By ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/department/{{departmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
