info:
  name: Get Department By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/department/{{departmentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
