info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: '{{baseUrl}}/auth/logout'
  auth: inherit

  script:
    postResponse: |-
      bru.setEnvVar("accessToken", "");

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
