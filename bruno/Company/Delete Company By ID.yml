info:
  name: Delete Company By ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/company/{{companyId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
