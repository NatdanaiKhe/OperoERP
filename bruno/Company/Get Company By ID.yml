info:
  name: Get Company By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/company/{{companyId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
