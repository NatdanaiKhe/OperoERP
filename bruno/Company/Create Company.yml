info:
  name: Create Company
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/company"
  body:
    type: json
    data: |-
      {
        "name": "Acme Corporation"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
