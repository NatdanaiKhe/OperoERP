info:
  name: Get Customer By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/customer/{{customerId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5