info:
  name: Delete Customer By ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/customer/{{customerId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5