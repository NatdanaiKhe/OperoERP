info:
  name: Get All
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/product"
  query:
    - name: name
      value:
    - name: sku
      value:
    - name: page
      value: "1"
    - name: limit
      value: "20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
