info:
  name: Update Product
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/product/{{productId}}"
  body:
    type: json
    data: |-
      {
        "name": "Standing Desk Pro",
        "defaultSalesPrice": 6400
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
