{
  "name": "database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "dependencies": {
    "@opero/config": "workspace:*",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "prisma": "^7.9.1"
  },
  "scripts": {
    "build": "infisical run --env=dev -- prisma generate && tsc -p tsconfig.json",
    "db:generate": "infisical run --env=dev -- prisma generate",
    "db:migrate-dev": "infisical run --env=dev -- prisma migrate dev",
    "db:deploy": "infisical run --env=dev -- prisma migrate deploy",
    "db:seed": "infisical run --env=dev -- prisma db seed",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
